name: PHP CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    uses: actions/checkout@v2
    uses: shivammathur/setup-php@v2
    with:
          php-version: '7.4'
    run: make install
    run: make lint